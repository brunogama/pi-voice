# Maintainers

Current maintainer:

- Bruno

Maintainers are responsible for reviewing changes that affect microphone capture, cloud upload behavior, temp audio cleanup, and prompt insertion safety.

## Release responsibilities

- Run smoke checks before tagging.
- Update `CHANGELOG.md`.
- Verify package contents with `npm pack --dry-run`.
- Tag releases as `vX.Y.Z` for git installs.
